name: Deploy Backend

on:
  push:
    branches: [production]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to cPanel via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: 194.15.36.113
          username: myugocom
          key: ${{ secrets.CPANEL_SSH_KEY }}
          script: |
            cd /home/myugocom/be_fieldbooking
            git pull origin production
