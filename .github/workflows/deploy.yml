name: Deploy Backend

on:
  push:
    branches: [production]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger cPanel deploy webhook
        run: |
          curl -s -X POST ${{ secrets.DEPLOY_WEBHOOK_URL }}
